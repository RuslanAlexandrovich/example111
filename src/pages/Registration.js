import React, { Component } from "react";
import RegistrComp from "../components/RegistrComp";

export default class Registration extends Component {
  render() {
    return (
      <div>
        <RegistrComp />
      </div>
    );
  }
}
